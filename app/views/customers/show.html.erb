 

<table class="customer-table">
 <tr>
   <th><strong>Customer Name: </strong></th>
   <th><strong>Customer Email: </strong></th>
</tr>
<tr>
  <td><%= @customer.name %></td>
  <td><%=  @customer.email %> </td>
</tr>


</table>
 
<div> 


<%if current_user.id == @customer.user_id %>
<%#the id is added to check whether the customer signed in is same as the customer id %>
<%# Though in reality the customer sign in will automatically update the customer table with name email and id.But since 
website has both the vendor and buyer option, the option for adding customer id is given which gets added through post request. %>

<%= button_to 'Edit information',   edit_customer_path, method: :get, class: "btn2" %>
<%= button_to 'Back',   new_customer_path, method: :get, class: "btn2" %>

 <ul> 
 <li><%= button_to "Delete", @customer,
                   method: :delete ,
                   class: "btn2",
                  data: { confirm: "Are you sure?" } %> </li>
</ul> 

<%end %>














